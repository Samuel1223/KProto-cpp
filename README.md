# KProto-cpp
Minimal in-memory k-prototypes clusterer in C++ — base for a SWE-bench task.
The distance primitives in `kproto::numeric_distance` / `categorical_distance`
/ `mixed_distance` are implemented; the clustering algorithm
`kproto::KPrototypes` is left to implement in src/kprototypes.cpp.
